<%  @answer = @question.answers.build if @answer.nil?%>
<%= form_for([@trip,@question,@answer]) do |f| %>
    <fieldset>
    <% if @answer.errors.any? %>
    <div id="error_explanation">
      <h2 class="alert alert-error"><%= pluralize(@answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% @answer.errors.full_messages.each do |msg| %>
        <li class="text-error"><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.text_area :body, {:placeholder=>"Help your friend, answer the question", :class=>"span12"}%>

  <div class="actions">
    <%= f.submit :class=>"btn btn-primary"%>
  </div>
  </fieldset>
<% end %>
